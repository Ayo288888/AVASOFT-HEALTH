# Auto-Push to Remote GitHub Rule

Whenever changes are made and committed in this repository:
1. After every successful change, stage and commit the modified files.
2. Automatically push the commits to the remote GitHub repository (`git push`) immediately following the commit.
3. Verify that `git status` reports `Your branch is up to date with 'origin/<branch>'`.
